# Pro Tools gRPC Dependencies
# Using latest versions with Python 3.14 support
grpcio>=1.68.0
grpcio-tools>=1.68.0
protobuf>=5.29.0

# Configuration Management
python-dotenv>=1.0.0

# GUI (optional - for desktop interface)
# Uncomment if building GUI version
# tkinter is built-in for Python on Mac

# Note: MASV transfers now handled by MASV Agent CLI
# The 'requests' library is no longer needed for MASV uploads
# Install MASV Agent from: https://developer.massive.io/transfer-agent/latest/
